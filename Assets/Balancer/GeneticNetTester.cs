using System;
using UnityEngine;

public class GeneticNetTester  {

    /*private int numberOfInputPerceptrons;
    private int numberOfOutputPerceptrons;
    private int numberOfHiddenPerceptrons;
    private int numberOfHiddenLayers;

    private int populationSize;

    public Net net;
    public GeneticNetTester(int numberOfInputPerceptrons, int numberOfOutputPerceptrons, int numberOfHiddenPerceptrons, int numberOfHiddenLayers, int populationSize) {
        this.numberOfInputPerceptrons = numberOfInputPerceptrons;
        this.numberOfOutputPerceptrons = numberOfOutputPerceptrons;
        this.numberOfHiddenPerceptrons = numberOfHiddenPerceptrons;
        this.numberOfHiddenLayers = numberOfHiddenLayers;

        this.populationSize = populationSize;
    }

    public void GenerateRandomNets() {
        net = new Net(numberOfInputPerceptrons, numberOfOutputPerceptrons, numberOfHiddenLayers, numberOfHiddenPerceptrons);
        net.SetRandomWeights();

    }

    public void OnFinished(object source, EventArgs e){
        Net net = (Net)source;
        Debug.Log(net.GetNetFitness()+" Fit");
    }*/


}
